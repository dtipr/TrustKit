import { Meta } from '@storybook/blocks';

<Meta title="Getting Started/Introduction" />

<div style={{ display: 'flex', justifyContent: 'center', marginBottom: '2rem' }}>
  <img src="/TrustKitLogo.png" alt="TrustKit Logo" style={{ maxWidth: '280px', height: 'auto' }} />
</div>

# TrustKit

A React UI component library for Keystone PKI/SSL/Identity products.

---

## Features

- **30+ Components** - Primitives, data display, navigation, forms, layout, and auth
- **Dark Sidebar** - Professional dark theme navigation
- **Tailwind CSS** - Utility-first styling with CSS custom properties
- **TypeScript** - Full type safety and IntelliSense
- **Tree-shakeable** - Import only what you need

---

## Installation

```bash
npm install @keystone/trustkit
```

---

## Quick Start

```tsx
import { Button, StatusBadge, DataTable, Sidebar } from '@keystone/trustkit';
import '@keystone/trustkit/styles.css';

function App() {
  return (
    <div className="flex h-screen">
      <Sidebar
        sections={[
          {
            title: 'Main Menu',
            items: [
              { id: 'dashboard', label: 'Dashboard', icon: LayoutDashboard },
              { id: 'accounts', label: 'Accounts', icon: Users },
            ],
          },
        ]}
        user={{ name: 'John Doe', email: 'john@example.com' }}
        activeItem="dashboard"
      />
      <main className="flex-1 p-6">
        <h1>Dashboard</h1>
        <StatusBadge status="active" />
      </main>
    </div>
  );
}
```

---

## Component Categories

### Primitives
Basic building blocks for your UI.
- `Button` - Primary, secondary, outline, ghost, destructive variants
- `Input` - Text input with icon and search variants
- `Textarea` - Multi-line text input
- `Checkbox` - Radix-based checkbox
- `Select` - Dropdown select with groups
- `Switch` - Toggle switch component
- `Avatar` - User avatar with auto-generated colors
- `ButtonGroup` - Segmented button group for filters

### Data Display
Components for presenting information.
- `StatusBadge` - Status indicators with colored dots
- `StatCard` - Metric cards with icon and value
- `MetricCard` - Grouped metrics with comparison badges
- `Card` - Container component with header/footer

### Navigation
Navigation and wayfinding components.
- `Sidebar` - Dark theme collapsible sidebar with search
- `Tabs` - Underlined tabs with count badges
- `Breadcrumbs` - Navigation breadcrumbs

### Forms
Form inputs and controls.
- `MultiSelect` - Multi-select with tag chips
- `DatePicker` - Calendar date picker
- `DateRangeFilter` - Date range with presets
- `FilterDropdown` - Checkbox filter with search
- `FormField` - Form field wrapper with label

### Layout
Layout and structure components.
- `Modal` - Dialog/modal component
- `Grid` - Responsive grid layout
- `ContactCard` - Contact information card
- `DataTable` - Sortable, paginated table with row selection
- `PageHeader` - Page title with actions

### Auth
Authentication components.
- `LoginForm` - Complete login form
- `RegisterForm` - Registration form with validation

---

## Design Tokens

TrustKit uses CSS custom properties for theming:

```css
:root {
  --tk-primary-500: #22c55e;      /* Green - Trust/Security */
  --tk-destructive-500: #ef4444;  /* Red - Errors */
  --tk-warning-500: #f59e0b;      /* Amber - Warnings */
  --tk-info-500: #3b82f6;         /* Blue - Information */
}
```

---

## Browse Components

Use the sidebar to explore all available components. Each component includes:

- **Interactive demos** - Try different props and variants
- **Code examples** - Copy-paste ready snippets
- **API documentation** - Full props reference

Start with the **Examples** section to see complete page layouts matching the design system.
